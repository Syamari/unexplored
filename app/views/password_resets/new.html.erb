<div class="container mx-auto my-4 py-4 px-4 rounded-lg flex items-center justify-center ">
  <div class="bg-gradient-to-r from-slate-400 to-purple-200 font-medium mx-auto max-w-sm py-4 px-4 border border-gray-500 rounded-lg shadow-lg m-auto">
      <h1 class="text-center text-2xl font-bold mb-6"><i class="fa-solid fa-user"></i> パスワードリセット申請</h1>
      <%= form_with url: password_resets_path do |f| %>
        <div class="mb-4">
					<%= f.label :メールアドレス %>
					<%= f.email_field :email, class: 'input input-bordered w-full', placeholder: "email@sample.com"  %>
        </div>
        <div class="mb-4">
          <%= f.submit '申請', class: 'btn btn-primary w-full' %>
        </div>
      <% end %>
      <p class="text-center text-rose-500 text-bold"><i class="fa-solid fa-circle-info"></i> ユーザー登録がまだの方は、以下のリンクからご登録できます</p>
      <div class='link-hover font-bold text-center text-xl py-2'>
        <%= link_to '登録ページへ', new_user_path %>
      </div>
  </div>
</div>