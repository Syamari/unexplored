<div class="container mx-auto my-4 py-4 px-4 rounded-lg flex items-center justify-center ">
  <div class="animate-fade-in bg-gradient-to-r from-slate-400 to-purple-200 font-medium mx-auto max-w-sm py-4 px-4 border border-gray-500 rounded-lg shadow-lg m-auto">
      <h1 class="text-center text-3xl font-bold mb-6"><i class="fa-solid fa-user"></i> ログイン</h1>
      <%= form_with url: login_path, local: true do |f| %>
        <div class="mb-4">
          <%= f.label :メールアドレス %>
          <%= f.text_field :email, class: 'input input-bordered w-full', placeholder: "email@sample.com" %>
        </div>
        <div class="mb-4">
          <%= f.label :パスワード %>
          <%= f.password_field :password, class: 'input input-bordered w-full', placeholder: "Password" %>
        </div>
        <div class="mb-4">
          <%= f.submit 'ログイン', class: 'btn btn-primary w-full' %>
        </div>
      <% end %>
      <p class="flex justify-center font-bold py-2">もしくは</p>
      <%= link_to auth_at_provider_path(provider: :google), class: "btn w-full" do %>
        <p class="text-primary"><i class="fa-brands fa-google"></i></p>
        Googleで ログイン / 登録
      <% end %>
      <div class="divider divider-neutral"></div>
      <p class="text-center font-bold text-sm text-slate-700"><i class="fa-solid fa-circle-info"></i> ユーザー登録がまだの方は以下から登録できます</p>
      <div class='link-hover font-bold text-center text-lg py-2'>
        <%= link_to '登録ページへ', new_user_path, class: 'btn btn-neutral w-full' %>
      </div>
      <div class="divider divider-neutral">Forgot Password ?</div>
      <div class='link-hover text-slate-900 font-bold text-center text-lg py-2'>
        <%= link_to 'パスワードをお忘れの方はこちら', new_password_reset_path, class: 'btn btn-neutral w-full' %>
      </div>
  </div>
</div>